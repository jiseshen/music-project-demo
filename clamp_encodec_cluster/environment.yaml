name: clamp_encodec_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      # audiocraft will be installed after cloning
      
# This is a minimal, clean environment for clamp_encodec
